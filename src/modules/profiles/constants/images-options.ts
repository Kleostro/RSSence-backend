import { ImageOptimizeOptions } from '@/shared/types/image-optimize';

export const AVATAR_OPTIONS: ImageOptimizeOptions = {
  width: 200,
  height: 200,
  quality: 80,
  format: 'webp',
  effort: 6,
};
